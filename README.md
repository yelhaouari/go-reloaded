# go-reloaded
Go tool for text editing and auto-correction. Converts hex/bin to decimal, changes word case, fixes punctuation and quotes, and adjusts “a/an”. Demonstrates Go file handling, string manipulation, and number conversion with unit tests.
